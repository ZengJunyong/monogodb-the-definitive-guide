// Generated by CoffeeScript 1.6.3
var insertPosts, post;

post = {
  title: 'My blog post',
  content: 'Here is my blog post.',
  date: new Date
};

db.blog.insert(post);

db.blog.find();

db.blog.findOne();

insertPosts = function() {
  var i, _i, _results;
  _results = [];
  for (i = _i = 1; _i <= 5; i = ++_i) {
    post = {
      title: 'My blog post' + i,
      content: 'Here is my blog post ' + i,
      date: new Date
    };
    _results.push(db.blog.insert(post));
  }
  return _results;
};

insertPosts();

post.comments = [];

db.blog.update({
  title: 'My blog post'
}, post);

db.blog.remove({
  title: 'My blog post5'
});
